<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xll.data.stream.manager.domain.aggregate.system.mapper.SysRoleMenuMapper">

	<resultMap type="com.xll.data.stream.manager.domain.aggregate.system.entity.SysRoleMenu" id="SysRoleMenuResult">
		<result property="roleId"     column="role_id"      />
		<result property="menuId"     column="menu_id"      />
	</resultMap>

	<select id="checkMenuExistRole" resultType="java.lang.Integer">
	    select count(1) from t_sys_role_menu where menu_id = #{menuId}
	</select>

	<delete id="deleteRoleMenuByRoleId" parameterType="java.lang.Long">
		delete from t_sys_role_menu where role_id=#{roleId}
	</delete>

	<delete id="deleteRoleMenu" parameterType="java.lang.Long">
 		delete from t_sys_role_menu where role_id in
 		<foreach collection="array" item="roleId" open="(" separator="," close=")">
 			#{roleId}
        </foreach>
 	</delete>

	<insert id="batchRoleMenu">
		insert into t_sys_role_menu(role_id, menu_id, half_checked) values
		<foreach item="item" index="index" collection="list" separator=",">
			(#{item.roleId},#{item.menuId},#{item.halfChecked})
		</foreach>
	</insert>

</mapper>
